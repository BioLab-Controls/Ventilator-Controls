import csv  

def updatePres(pdata,tdata):
    
    pres = []
    t = []	    
    
    pres.append(pdata)
    t.append(tdata)

with open('countries.csv', 'w', encoding='UTF8') as f:
    writer = csv.writer(f)

    # write the header
    writer.writerow(pres)

    # write the data
    writer.writerow(t)
